{
  "name": "framemaker",
  "version": "1.0.0",
  "description": "1.npm init构建package.json 2.在package.json中添加bin字段 3.在js文件开头添加 #!node 4.运行 npm install . -g 5.运行自定义的命令 zlcmd -i 1",
  "main": "app.js",
  "bin": {
    "framemaker": "app.js"
  },
  "dependencies": {
    "commander": "^2.9.0",
    "fs-extra": "^0.26.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
